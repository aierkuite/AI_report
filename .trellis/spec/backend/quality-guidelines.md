# Quality Guidelines

> Code quality standards for backend development.

---

## Overview

<!--
Document your project's quality standards here.

Questions to answer:
- What patterns are forbidden?
- What linting rules do you enforce?
- What are your testing requirements?
- What code review standards apply?
-->

(To be filled by the team)

---

## Forbidden Patterns

<!-- Patterns that should never be used and why -->

(To be filled by the team)

---

## Required Patterns

<!-- Patterns that must always be used -->

(To be filled by the team)

---

## Testing Requirements

<!-- What level of testing is expected -->

(To be filled by the team)

---

## Code Review Checklist

<!-- What reviewers should check -->

(To be filled by the team)

---

## Local ML CLI Script Contracts

### Scenario: Qwen LoRA answer comparison CLI

#### 1. Scope / Trigger
- Trigger: adding or changing a local Python command-line script that compares a base Qwen model with a LoRA adapter.
- Applies to `qwen_lora_compare.py`.
- The command is a local inspection tool, not a training script and not a batch evaluation framework.

#### 2. Signatures
- Positional form: `python qwen_lora_compare.py [instruction] [input]`.
- Option form: `python qwen_lora_compare.py --instruction <text> --input <text>`.
- Optional runtime flags: `--model-dir`, `--adapter-dir`, `--device`, `--max-new-tokens`, `--temperature`, `--top-p`, `--do-sample`, `--repetition-penalty`, `--system-prompt`.
- There must be no `--output` argument. The output is generated by the compared models.

#### 3. Contracts
- `instruction` is required before inference begins. If it is missing, the script may prompt interactively.
- `input` is optional and may be an empty string.
- The model prompt must contain only the instruction and optional input, formatted through the Qwen chat template.
- The script must print the instruction, input, base-model answer, and LoRA-adapter answer.
- The script must decode only newly generated tokens and must not print chat template markers such as `<|im_start|>` or `<|im_end|>`.

#### 4. Validation & Error Matrix
- Missing base model directory -> raise `FileNotFoundError` with the path.
- Missing LoRA adapter directory -> raise `FileNotFoundError` with the path.
- Missing `torch`, `transformers`, or `peft` -> raise `RuntimeError` with the dependency name.
- Requested `--device cuda` while CUDA is unavailable -> raise `RuntimeError`.
- Empty instruction after interactive prompting -> raise `ValueError`.

#### 5. Good / Base / Bad Cases
- Good: `python qwen_lora_compare.py --instruction "请用一句话解释什么是人工智能" --input "" --device cpu --max-new-tokens 80`.
- Base: `python qwen_lora_compare.py "请用一句话解释什么是人工智能" ""`.
- Bad: `python qwen_lora_compare.py --instruction "..." --output "标准答案"` because the comparison tool must not ask the user for expected output.

#### 6. Tests Required
- Syntax check: `python -m py_compile qwen_lora_compare.py`.
- Help check: `python qwen_lora_compare.py --help` must list `instruction` and `input`, and must not list `--output`.
- Argument parsing check: `parse_args()` must produce `instruction` and `input`, and must not produce an `output` field.
- Encoding check: the file must be UTF-8 without BOM and use CRLF line endings on Windows.

#### 7. Wrong vs Correct

Wrong:
```bash
python qwen_lora_compare.py --instruction "解释人工智能" --input "" --output "人工智能是..."
```

Correct:
```bash
python qwen_lora_compare.py --instruction "解释人工智能" --input ""
```
